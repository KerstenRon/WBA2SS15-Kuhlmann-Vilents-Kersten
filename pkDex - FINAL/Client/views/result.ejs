<!DOCTYPE html>
<html>
    <head>
        <title>Tabula RaZa's Pokedex</title>
        <meta name="description" content="Der Pokédex von Tabula RaZa">
        <meta charset="utf-8">
        
        <link href="css/style.css" rel="stylesheet" type="text/css">    
    </head>
    
    <!-- BODY -->

    <body>

        <!-- NAVIGATIONSLEISTE -->
        <nav>
            <ul>
                <li>
                    <div name="home">
                        <a href="/">Home</a>
                    </div>
                </li>
                <li>
                    <div name="user">
                        <a href="/pkUser">Profil</a>
                    </div>
                </li>
                <li>
                    <div name="impressum">
                        <a href="/about">Impressum</a>
                    </div>
                </li>
            </ul>
        </nav>
    
        <!-- DEX -->
        <div class="dex">
                
            <!--LINKER DATENSCHIRM-->
            <!--Auswahlmöglichkeiten / Interaktion-->
            <!--Links MÜSSEN individuelles Padding in Abhängigkeit von der Textlänge bekommen, -->
            <!-- weil sonst die ganze Schaltfläche nicht als Link benutzt werden kann...-->
            <div class="data-left">
                <%  var i = 0;
                    var length = Object.keys(pkUser).length;
                    
                    while(i < length) {
                        if (pkUser[0].user[5].lastOpponent == pkUser[i].user[0].sign) {
                            break;
                        }
                        i++;
                    }
                    var score = pkUser[0].user[2].hscore;
                    var oppscore = pkUser[i].user[2].hscore;
                %>
                <% if (score > oppscore) { %>
                    <h3 class="dl-title">Gewonnen!</h3>
                    <p class="dl-text">Glückwunsch, Du hast gewonnen!</p>
                <% } else { %>
                    <h3 class="dl-title">Verloren...</h3>
                    <p class="dl-text">Schade, du konntest deinen Gegner nicht schlagen.</p>
                <% } %>
                
                <div class="dl-btn" id="start">
                    <a href="/click" style="padding: 26px 80px 27px">Neuer Clickfight</a>
                </div>

                <div class="dl-btn" id="back">
                    <a href="/" style="padding: 26px 80px 37px;">Zurück zum Menü</a>
                </div>
            </div>
        
            <!--RECHTER DATENSCHIRM-->
            <!--Auflistung von Informationen-->
            <div class="data-right">
            
                <div class="nftitle" id="title">
                    <p>Dein Gegner: </p>
                </div>
                <div class="profile-pic">
                    <img src="<%=pkUser[i].user[4].pic%>" />
                </div>
                <p style="text-align: center"><%=pkUser[i].user[1].name%></p>
                <p style="text-align: center"><%=pkUser[i].user[2].hscore%></p>
            </div>
    
        </div>
        
        <footer>© Tabula RaZa 2015</footer>

    </body>
</html>